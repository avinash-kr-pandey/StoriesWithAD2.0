"use client";

const HeaderBanner = () => {
  return (
    <div className="w-full h-64 bg-gray-300 flex items-center justify-center m-0 p-0">
      {/* Content (optional) */}
    </div>
  );
};

export default HeaderBanner;
